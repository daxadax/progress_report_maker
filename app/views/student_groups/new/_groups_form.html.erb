<div class="holder round">
	
<h3>Group information:</h3>


<%= form_for @student_group do |f| %>

  <div class="holder2 round">
    The name of this group is 
    <span class="field form_field"><%= f.text_field :name %></span>
    and it is a/an 
    <!-- FIX THIS -->
    <span class="field dropdown">
      <%= f.select :type_of_group, StudentGroup::VALID_TYPES, prompt: "select group type" %>
    </span> 	  
  </div>

  <h3>
		Student information:
	</h3>

  <div class="holder2 round">
    <%= render "student_groups/new/student_fields", :f => f %>
  </div>

</div>
<p>
	<%= f.submit "Submit", :class => 'big_button round unselectable'%>
</p>

<% end %>