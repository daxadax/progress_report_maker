<table class="student_groups_index">
  
  <% @student_groups.each do |group| %>
	   <tbody> 
	    <tr>
	    	<th>
	        <%= link_to group.name, class_path(group) %> <%= link_to "Edit", edit_user_student_group_path(@user, group), class: "lil_link" %>
		    </th>
	    </tr>
		  <tr>
		  	<td> # of students </td>
		    <td> subjects studied </td>
		    <td> length of course </td>
		    <td> goals </td>
		  </tr>
		</tbody>
  <% end %>

</table>