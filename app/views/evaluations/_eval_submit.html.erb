<% if (@student_number.to_f / @student_group.students.count) != 1 %>
  <%= f.submit "Next student", :class => 'big_button round unselectable eval_submit' %>
<% else %>
  <%= f.submit "Finish!", :class => 'big_button round unselectable eval_submit' %>
<% end %>