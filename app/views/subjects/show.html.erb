<div class="holder round">

<h3>Members of <span class="name"><%= @subject.name %></span> in <span class="name"><%= @subject.student_group.name %></span> should:</h3> 

	  <div class="holder2 round">
	  	<%= render 'subject_show_goals' %>
	  </div>

</div>