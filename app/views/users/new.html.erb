<%= form_for(@user) do |f| %>

<div class="form_section round">

  <%= render 'fields', :f => f  %>

	<div>
	  <%= f.submit "Get Started!", :class => 'big_button round unselectable' %>
	</div>

</div>

<% end %>